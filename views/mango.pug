extends layout

block content
  h1= title
  br
  - let mango = results
  
   div(class = "container-fluid")
    div(class = "row m-1")
     each i,index in mango
      -let destination = `dispatch(\"/mango/update?id=${i._id}\")`
     
        div(class="row-sm-2", class="m-1") Item #{i.type}(#{i.duration}) is #{i.cost} 
            button.btn( class="btn-primary ml-1", onclick= destination) Update 
        script.
                function buy(itemNo){
                    alert(`You purchased item ${itemNo+1}`)
                }
                function dispatch(path){ 
                window.location.href=path 
                }